<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head.ejs") %>
    <script src="https://unpkg.com/validator@latest/validator.min.js"></script>
    <script defer src="/scripts/register.js"></script>

    <body>
        <%- include("../partials/header.ejs") %>
            <main>
                <section class="section-register">
                    <h1>Crear cuenta</h1>
                    <p>¡Comprá en Zaphir y llevá el control de tus pedidos en un solo lugar!</p>
                </section>
                <form action="/usuario/registro" method="post" id="form">
                    <fieldset class="form-name">
                        <label for="nombre">Nombre y apellido</label>
                        <input type="text" name="nombre" id="nombre" placeholder="ej: Juan Perez"
                            value="<%= locals.errors ? oldData.nombre : null %>">
                        <p class="feed"></p>
                    </fieldset>
                    <fieldset class="form-email">
                        <label for="email">Email</label>
                        <input type="text" name="email" id="email" placeholder="ej: tunombre@email.com"
                            value="<%= locals.errors ? oldData.email : null %>">
                        <p class="feed"></p>
                    </fieldset>
                    <fieldset class="form-tel">
                        <label>Teléfono (opcional)</label>
                        <input type="tel" name="telefono" id="tel" placeholder="ej: 1123445567">
                        <p class="feedTel"></p>
                    </fieldset>
                    <fieldset class="form-password">
                        <label for="password">Contraseña</label>
                        <input type="password" name="password" id="password"
                            value="<%= locals.errors ? oldData.password : null %>">
                        <p class="feed"></p>
                    </fieldset>
                    <fieldset class="form-confirm">
                        <label for="contraseña">Confirmar contraseña</label>
                        <input type="password" name="passwordConfirm" id="confirm-password"
                            value="<%= locals.errors ? oldData.passwordConfirm : null %>">
                        <p class="feed"></p>
                    </fieldset>
                    <fieldset class="form-button">
                        <button>Crear cuenta</button>
                    </fieldset>
                    <fieldset class="form-linklogin">
                        <a href="/usuario/ingresar">¿Ya tenés una cuenta?<span>Iniciá sesión</span></a>
                    </fieldset>
                </form>
                <%- include("../partials/logo-wts.ejs") %>
            </main>
            <%- include("../partials/footer.ejs") %>
    </body>

</html>